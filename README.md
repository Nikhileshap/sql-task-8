# SQL Developer Internship - Task 8: Stored Procedures and Functions

## 🎯 Objective

To create and utilize **Stored Routines** (Procedures and Functions) to learn modular, reusable, and parameter-driven SQL logic. 

---

## ✅ Key Concepts Demonstrated

* **Stored Procedure (`AddNewProduct`):**
    * Demonstrates encapsulation of **DML** (`INSERT`) logic.
    * Uses **`IN` parameters** to pass data into the routine.
* **User-Defined Function (`GetCustomerFullName`):**
    * Demonstrates a **scalar function** that **`RETURNS`** a single value (the full name).
    * Used **`CONCAT`** to format the output.
    * Demonstrated calling the function directly within a `SELECT` query.

